// processs is a variable that gives us information about the currently running thing
// console.log(process.argv[2]);
const firstArgument = process.argv[2]; // grabbing the first argument and setting it to a varaible
const secondArgument = process.argv[3]; // grabbing the second argument and setting it to a variable
console.log(`Our first argumemt os" ${firstArgument}`)
console.log(`Our second argument is: ${secondArgument}`)
